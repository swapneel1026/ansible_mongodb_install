---
- name: Install MongoDB package
  apt:
    name: mongodb
    state: present
    update_cache: yes
  when: ansible_os_family == "Debian"

- name: Install MongoDB package
  yum:
    name: mongodb
    state: present
  when: ansible_os_family == "RedHat"
